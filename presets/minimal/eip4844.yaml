# Minimal preset - Phase0

# Misc
# ---------------------------------------------------------------
# [customized]
FIELD_ELEMENTS_PER_BLOB: 4
# `uint64(32)`
BYTES_PER_FIELD_ELEMENT: 32
# `uint64(2**4)` (= 16)
MAX_BLOBS_PER_BLOCK: 16
